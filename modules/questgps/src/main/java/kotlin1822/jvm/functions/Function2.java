package kotlin1822.jvm.functions;

/**
 * 编译期占位：运行时由 TabooLib 重定位的 {@code kotlin1822.jvm.functions.Function2} 提供。
 * 打包时通过 build.gradle.kts 排除，不会进入最终 JAR。
 */
public interface Function2<P1, P2, R> {
    R invoke(P1 p1, P2 p2);
}
