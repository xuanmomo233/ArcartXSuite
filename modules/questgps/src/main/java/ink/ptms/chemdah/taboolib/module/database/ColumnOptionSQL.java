package ink.ptms.chemdah.taboolib.module.database;

/**
 * 编译期占位：运行时由 TabooLib 重定位的 module-database 提供。
 * 打包时通过 build.gradle.kts 排除，不会进入最终 JAR。
 * 仅声明本实现引用到的常量，运行时使用真实枚举字段。
 */
public enum ColumnOptionSQL {
    KEY,
    UNIQUE_KEY
}
