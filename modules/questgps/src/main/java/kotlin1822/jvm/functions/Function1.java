package kotlin1822.jvm.functions;

/**
 * 编译期占位：运行时由 TabooLib 重定位的 {@code kotlin1822.jvm.functions.Function1} 提供。
 * 打包时通过 build.gradle.kts 排除，不会进入最终 JAR。
 */
public interface Function1<P1, R> {
    R invoke(P1 p1);
}
